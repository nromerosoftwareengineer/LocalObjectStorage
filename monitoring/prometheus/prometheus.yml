global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: local-object-storage-api
    metrics_path: /prometheus
    static_configs:
      - targets:
          - host.docker.internal:8080
